/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['via.placeholder.com', 'lh3.googleusercontent.com', 'encrypted-tbn0.gstatic.com'], // Add others if needed
  },
};

module.exports = nextConfig;
